[package]
name = "sensor-graph"
version = "0.1.0"
edition = "2021"
description = "Wayland-native live temperature graph (psensor replacement)"

[dependencies]
gtk4 = "0.9"
serde_json = "1"

[profile.release]
opt-level = 2
